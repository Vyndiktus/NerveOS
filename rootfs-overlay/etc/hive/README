HiveOS runtime config directory.

Files populated here on first boot:
  wg-private.key   — WireGuard private key (generated by hived)
  ca.crt           — Hive CA certificate (installed during enrollment)
  hived.conf       — Node configuration (copied from device default)
