NerveOS runtime config directory.

Files populated here on first boot:
  wg-private.key   — WireGuard private key (generated by nerved)
  ca.crt           — Hive CA certificate (installed during enrollment)
  nerved.conf       — Node configuration (copied from device default)
